import 'package:flutter/material.dart';

// Renk Standardizasyonu
const Color fullDayColor = Color(0xFF4F5FBF); // Koyu mavi (Tam Gün)
const Color halfDayColor = Color(0xFF8B9FE8); // Açık mavi (Yarım Gün)
const Color primaryIndigo = Color(0xFF4338CA); // Primary renk
const Color errorColor = Color(0xFFE89595); // Açık kırmızı/pembe (Gelmedi)
