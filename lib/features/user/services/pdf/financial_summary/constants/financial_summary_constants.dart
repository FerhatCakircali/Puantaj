import 'package:pdf/widgets.dart' as pw;

/// Finansal özet raporu için sabitler
class FinancialSummaryConstants {
  static const pageMargin = pw.EdgeInsets.all(32);
  static const cardPadding = pw.EdgeInsets.all(20);
  static const sectionSpacing = 30.0;
  static const cardSpacing = 16.0;
}
