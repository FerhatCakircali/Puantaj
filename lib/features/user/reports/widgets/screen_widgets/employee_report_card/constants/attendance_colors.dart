import 'package:flutter/material.dart';

/// Devam durumu renk sabitleri
class AttendanceColors {
  /// Tam gün rengi (koyu mavi)
  static const Color fullDay = Color(0xFF4F5FBF);

  /// Yarım gün rengi (açık mavi)
  static const Color halfDay = Color(0xFF8B9FE8);

  /// Gelmedi rengi (açık kırmızı)
  static const Color absent = Color(0xFFE89595);
}
